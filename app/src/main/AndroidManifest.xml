<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lentimosystems.licio.blesbokandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PaymentActivity"></activity>
        <activity
            android:name=".TsavoActivity"
            android:theme="@style/AppTheme2" />
        <activity
            android:name=".NanyukiActivity"
            android:theme="@style/AppTheme2" />
        <activity
            android:name=".NakuruActivity"
            android:theme="@style/AppTheme2" />
        <activity
            android:name=".AmboseliActivity"
            android:theme="@style/AppTheme2" />
        <activity
            android:name=".SamburuActivity"
            android:theme="@style/AppTheme2" />
        <activity
            android:name=".MaraActivity"
            android:theme="@style/AppTheme2" />

        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".HomeActivity" />

        <activity
            android:name=".HomeActivity"
            android:theme="@style/AppTheme2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>