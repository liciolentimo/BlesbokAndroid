<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lentimosystems.licio.blesbokandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TsavoActivity"></activity>
        <activity android:name=".NanyukiActivity" />
        <activity android:name=".NakuruActivity" />
        <activity android:name=".AmboseliActivity" />
        <activity android:name=".SamburuActivity" />
        <activity android:name=".MaraActivity" />
        <activity
            android:name=".HomeActivity"
            android:theme="@style/AppTheme2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>